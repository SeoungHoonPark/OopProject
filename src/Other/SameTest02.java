package Other;

/**
 * 3
 *	같은 패키지에 있으면서 상속을 받은 경우 어디까지 사용할 수 있는가?
 */
public class SameTest02 extends MyClass01 {
		// 4개의 변수가 보인다.
		void test(){
			// 이 클래스는 이미 MyClass01을 상속 받았으므로
			// 4개의 변수가 모두 복사되어 잏다.
			
			//pulbic 모든 클래스에서 사용할 수 있기 때문에
			pub = 1000;
			
			// 상속받았거나 같은 패키지에 소속되면 사용할 수 있기 때문
			pro = 2000;
			
			// 같은 패키지에 소속되면 사용할 수 있기 때문에
			pack = 3000;
			// ★★
			// 아무리 상속 받았더라도 private는 사용 할 수  없다.
			// pri = 4000;
		}
}
